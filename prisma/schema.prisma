generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model tempLog {
  id        Int      @id @default(autoincrement())
  log       String
  createdAt DateTime @default(now())
}
